import { CheckCircle2, XCircle } from 'lucide-react'; import DistanceMeter from './DistanceMeter';
export default function VerificationResult({result,onReset}) { return <section className={`result ${result.is_genuine?'genuine':'forged'}`}><div className="result-title">{result.is_genuine?<CheckCircle2/>:<XCircle/>}<h2>{result.is_genuine?'Genuine Signature':'Potential Forgery'}</h2></div><div className="stats"><div><small>Similarity Score</small><strong>{result.similarity_score.toFixed(1)}%</strong></div><div><small>Distance</small><strong>{result.distance.toFixed(4)}</strong></div><div><small>Threshold</small><strong>{result.threshold.toFixed(4)}</strong></div></div><DistanceMeter distance={result.distance} threshold={result.threshold}/><p>{result.is_genuine?'The submitted signature is sufficiently similar to the reference according to the model.':'The submitted signature differs from the reference according to the trained model.'}</p><button className="secondary" onClick={onReset}>Verify Another Pair</button></section>; }
