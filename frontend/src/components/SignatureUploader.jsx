import { UploadCloud } from 'lucide-react';
import SignaturePreview from './SignaturePreview';
export default function SignatureUploader({label,file,onFile,onRemove}) { const choose=e=>onFile(e.target.files?.[0]); const drop=e=>{e.preventDefault(); onFile(e.dataTransfer.files?.[0]);}; return <div className="upload"><label>{label}</label>{file?<div className="selected"><SignaturePreview file={file}/><div><b>{file.name}</b><small>{(file.size/1024).toFixed(1)} KB</small></div><button onClick={onRemove}>Remove</button></div>:<label className="drop" onDragOver={e=>e.preventDefault()} onDrop={drop}><UploadCloud size={32}/><span>Drop an image here or <b>browse</b></span><small>PNG, JPG or JPEG · max 5 MB</small><input type="file" accept="image/png,image/jpeg" onChange={choose}/></label>}</div>; }
