const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:8000';
export async function verifySignature(referenceFile, testFile) { const form = new FormData(); form.append('reference_signature', referenceFile); form.append('test_signature', testFile); let response; try { response = await fetch(`${API_URL}/api/verify`, {method:'POST', body:form}); } catch { throw new Error('Unable to connect to the verification server.'); } const body = await response.json().catch(()=>({})); if (!response.ok) throw new Error(body.detail || (response.status === 413 ? 'File size is too large. Maximum allowed size is 5 MB.' : response.status >= 500 ? 'Signature verification failed. Please try again.' : 'Please upload two valid signature images.')); return body; }
export async function health() { const response = await fetch(`${API_URL}/api/health`); return response.json(); }
